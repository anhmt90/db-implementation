1. Import the project in Eclipse IDE. 
2. Put the tbl-files into the folder named "tbl" so that the program can read them.
3. main() function is in cpp/main.cpp.
4. The newOrder and the dilivery transactions as well as the query is written as functions in main.cpp.
5. The programm is just built and tested by Eclipse IDE on Xubuntu 16.04 LTS.